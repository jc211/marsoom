[project]
name = "marsoom"
readme = "README.md"
version = "1.0"
description = "A 3D drawing tool"
requires-python = ">=3.10"
dependencies = [
    "imgui-bundle",
    "pyglet",
    "warp-lang",
    "torch",
    "numpy",
]
